
  # EcoRewards

  This is a code bundle for EcoRewards web app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  visit [EcoRewards](https://ecoreward.netlify.app/) to explore the platform.